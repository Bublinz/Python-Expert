# write a program that will calculate the area of a traingle
print('calculate the area of a triangle')
def rate(length): 
    b = int(input('Input the Base: '))
    h = int(input('Input the Height: '))
    # area = 0.5 * b * h
    area = 0.5 * b * h
    print('The area of the triangle is ' + str(area))
rate('length')
